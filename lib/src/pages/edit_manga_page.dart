import 'package:flutter/material.dart';

class EditMangaPage extends StatelessWidget {
  const EditMangaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
